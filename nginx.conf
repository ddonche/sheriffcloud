worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  text/html;
    sendfile      on;
    keepalive_timeout 65;

    map $http_user_agent $is_crawler {
        default                         0;
        ~*facebookexternalhit           1;
        ~*Twitterbot                    1;
        ~*LinkedInBot                   1;
        ~*Slackbot                      1;
        ~*TelegramBot                   1;
        ~*WhatsApp                      1;
        ~*Discordbot                    1;
        ~*Googlebot                     1;
        ~*bingbot                       1;
        ~*iframely                      1;
        ~*Embedly                       1;
    }

    # Unified app bundle: Sheriff front, Admin, Spur front
    server {
        listen 8080;
        server_name
            sheriffcloud.com
            www.sheriffcloud.com
            admin.sheriffcloud.com
            spur.sheriffcloud.com
            spur.ink
            www.spur.ink;

        root /app/dist;
        index index.html;
        port_in_redirect off;

        location /api/ {
            proxy_pass http://127.0.0.1:9000;
            proxy_set_header Host $host;
            proxy_http_version 1.1;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location / {
            try_files $uri $uri/ $uri.html /index.html;
        }
    }

    # Chatterbox (still separate for now)
    server {
        listen 8080;
        server_name chatterbox.sheriffcloud.com;
        root /app/dist/chatterbox;
        index index.html;
        port_in_redirect off;

        location / {
            try_files $uri $uri/ /index.html;
        }
    }

    # Holster (still separate for now)
    server {
        listen 8080;
        server_name holster.sheriffcloud.com;
        root /app/dist/holster;
        index index.html;
        port_in_redirect off;

        location / {
            try_files $uri $uri/ /index.html;
        }
    }

    # Published portal sites on sheriffcloud.com
    server {
        listen 8080;
        server_name ~^(?<sub>[^.]+)\.sheriffcloud\.com$;
        root /app/dist;
        index index.html;
        port_in_redirect off;

        gzip on;
        gzip_types text/plain text/css application/javascript application/json image/svg+xml application/rss+xml;
        gzip_min_length 1024;

        resolver 1.1.1.1 valid=60s;

        location = /rss {
            rewrite ^ /functions/v1/rss_feed break;
            proxy_pass https://ukyjfstfoaybvzwplrwx.supabase.co;
            proxy_set_header Host ukyjfstfoaybvzwplrwx.supabase.co;
            proxy_set_header X-Subdomain $sub;
            proxy_set_header Authorization "Bearer sb_publishable_rscjPXRJKAm8ZSTRXr_wZw_-umwFzDD";
            proxy_set_header apikey "sb_publishable_rscjPXRJKAm8ZSTRXr_wZw_-umwFzDD";
            proxy_http_version 1.1;
            proxy_ssl_server_name on;
            proxy_read_timeout 10s;
        }

        location /assets/ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }

        location / {
            try_files $uri $uri/ /index.html;
        }
    }

    # Published Spur blog sites on spur.ink
    server {
        listen 8080;
        server_name ~^(?<sub>[^.]+)\.spur\.ink$;
        root /app/dist;
        index index.html;
        port_in_redirect off;

        gzip on;
        gzip_types text/plain text/css application/javascript application/json image/svg+xml application/rss+xml;
        gzip_min_length 1024;

        resolver 1.1.1.1 valid=60s;

        location = /rss {
            rewrite ^ /functions/v1/rss_feed break;
            proxy_pass https://ukyjfstfoaybvzwplrwx.supabase.co;
            proxy_set_header Host ukyjfstfoaybvzwplrwx.supabase.co;
            proxy_set_header X-Subdomain $sub;
            proxy_set_header Authorization "Bearer sb_publishable_rscjPXRJKAm8ZSTRXr_wZw_-umwFzDD";
            proxy_set_header apikey "sb_publishable_rscjPXRJKAm8ZSTRXr_wZw_-umwFzDD";
            proxy_http_version 1.1;
            proxy_ssl_server_name on;
            proxy_read_timeout 10s;
        }

        location /assets/ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }

        location ~ ^/blog/(.+)$ {
            if ($is_crawler) {
                proxy_pass http://127.0.0.1:9001/og/$sub$request_uri;
                break;
            }
            try_files $uri $uri/ /index.html;
        }

        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}
